// Write a function that takes two arguments. This function should return the concatenated value of the two arguments.

const stringSmasher = (firstString, secondString) => {
    const joinedNames = `${firstString} ${secondString}`
    return joinedNames
}

// Call the function with your first name and last name as the two arguments.
stringSmasher("Kitty Purry", "biggest Fan")

// Console log the return value.
console.log(joinedNames)


